services:
  - type: web
    name: remotion-render-api
    runtime: docker
    plan: starter
    envVars:
      - key: PORT
        value: "3000"
      - key: RENDER_TOKEN
        sync: false
      - key: BASE_URL
        sync: false
    healthCheckPath: /
    autoDeploy: true
